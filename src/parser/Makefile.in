BISON = @YACC@
FLEX  = @LEX@

all: y.tab.c y.tab.h lex.yy.c

y.tab.c y.tab.h: query.yacc
	$(YACC) -t -d query.yacc -o y.tab.c --defines=y.tab.h

lex.yy.c: query.lex
	$(FLEX) -o lex.yy.c -i query.lex

test:
	./test_memory_leaks.sh
