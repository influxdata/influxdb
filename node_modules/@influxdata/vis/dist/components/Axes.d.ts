import { FunctionComponent, CSSProperties } from 'react';
import { Margins, Scale, SizedConfig } from '../types';
import { PlotEnv } from '../utils/PlotEnv';
interface Props {
    env: PlotEnv;
    style: CSSProperties;
}
interface DrawAxesOptions {
    canvas: HTMLCanvasElement;
    innerWidth: number;
    innerHeight: number;
    margins: Margins;
    xTicks: number[];
    yTicks: number[];
    xTickFormatter: (tick: number) => string;
    yTickFormatter: (tick: number) => string;
    xScale: Scale<number, number>;
    yScale: Scale<number, number>;
    config: SizedConfig;
}
export declare const drawAxes: ({ canvas, innerWidth, innerHeight, margins, xTicks, yTicks, xTickFormatter, yTickFormatter, xScale, yScale, config: { width, height, axisColor, axisOpacity, gridColor, gridOpacity, tickFont, tickFontColor, xAxisLabel, yAxisLabel, }, }: DrawAxesOptions) => void;
export declare const Axes: FunctionComponent<Props>;
export {};
