import { Scale } from '../types';
import { DragEvent } from '../utils/useDragEvent';
export declare const getRectDimensions: (event: DragEvent, plotWidth: number, plotHeight: number) => {
    x: number;
    y: number;
    width: number;
    height: number;
};
export declare const rangeToDomain: ([p0, p1]: number[], scale: Scale<number, number>, length: number) => number[];
