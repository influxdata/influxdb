import { ColumnType } from '../types';
import { TextMetrics } from './getTextMetrics';
export declare const getTicks: (domain: number[], length: number, orientation: "vertical" | "horizontal", columnType: ColumnType, charMetrics: TextMetrics) => number[];
