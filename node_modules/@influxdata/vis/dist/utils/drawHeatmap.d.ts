import { Table, Scale } from '../types';
interface DrawHeatmapOptions {
    canvas: HTMLCanvasElement;
    width: number;
    height: number;
    table: Table;
    xScale: Scale<number, number>;
    yScale: Scale<number, number>;
    fillScale: Scale<number, string>;
    rows?: number[];
    brighten?: number;
}
export declare const drawHeatmap: ({ canvas, width, height, table, xScale, yScale, fillScale, rows, brighten, }: DrawHeatmapOptions) => void;
export {};
