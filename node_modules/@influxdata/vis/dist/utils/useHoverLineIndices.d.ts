import { Scale, NumericColumnData } from '../types';
export declare const useHoverLineIndices: (mode: "x" | "y" | "xy", mouseX: number, mouseY: number, xColData: NumericColumnData, yColData: NumericColumnData, groupColData: string[], xScale: Scale<number, number>, yScale: Scale<number, number>, width: number, height: number) => number[];
