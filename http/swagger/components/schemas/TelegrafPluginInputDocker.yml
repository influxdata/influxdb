type: object
required:
  - name
  - type
  - config
properties:
  name:
    type: string
    enum: ["docker"]
  type:
    type: string
    enum: ["input"]
  comment:
    type: string
  config:
    $ref: "./TelegrafPluginInputDockerConfig.yml"
